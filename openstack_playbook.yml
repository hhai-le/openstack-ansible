---
- hosts: all
  become: yes
  become_user: root
  become_method: sudo
  vars_files:
    - vars.yml
  tasks:
  - name: install openstack
    include_role:
      name: roles/controller
    when: controller
